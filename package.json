{
  "name": "preact-boilerplate",
  "version": "1.0.0",
  "description": "A minimal Preact boilerplate project, powered by Parcel.",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "parcel public/index.html --no-cache --out-dir build --port 3000 --open",
    "build": "parcel build public/index.html --no-cache --out-dir build"
  },
  "dependencies": {
    "preact": "^10.0.0"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4"
  }
}
